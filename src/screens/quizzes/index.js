import React, {Component} from 'react';
import {Text, View} from 'react-native';

export default class QuizzesScreen extends Component {
  render() {
    return (
      <View>
        <Text> List quizzes from platform </Text>
      </View>
    );
  }
}
